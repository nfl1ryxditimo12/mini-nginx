server {
    listen         9090;
    server_name    localhost;

    index index.html;

    location / {
        limit_except GET;
        index index.html;
    }

    location /post_body {
        limit_except POST;
        client_max_body_size 100;
    }

    location /directory {
        limit_except GET;
        root YoupiBanane;
        index youpi.bad_extension;
    }

    location /naver {
        return 301 https://naver.com;
    }
}
